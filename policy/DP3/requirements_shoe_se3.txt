# Install on top of RoboTwin's CUDA-matched PyTorch environment.
# PyTorch itself remains pinned by ../../script/requirements.txt so this file
# cannot accidentally replace a working CUDA build with a CPU wheel.
h5py>=3
numpy>=1.23,<2
trimesh==4.4.3
zarr>=2.16,<3
diffusers==0.11.1
hydra-core==1.3.2
omegaconf==2.3.0
