# 稿件一致性检查

本轮以 revised draft 为修改对象，保留用户提供的实验数字。

## 已调整

- 删除 Introduction 与 Method 中尚无对应结果的受控噪声实验声明。
- 明确训练中的 view 是旋转和坐标扰动，不等同于相机遮挡或独立可见表面。
- 修复真实实验图片的仿真图注，将图片移至真实实验小节；仿真描述引用正确图片。
- 区分仿真每个 seed 的 100 次测试和真实实验每任务 20 次测试。
- 增加 G3Flow 仿真表占位和实验说明，现有比较结论仅涵盖已完成的方法。
- 区分 DINOv2 lifting 对照和完整 G3Flow 实现，不预设后者共享全部内部处理流程。
- 将定性图与噪声因果解释分开；跨 seed 方差不作为视角融合不稳定的直接证据。
- 明确 query 采样消融保留 field，不等同于同监督条件下 field 与逐点模型的比较。
- 对一致性损失 0.4 个百分点差异不作显著性判断。
- 修正旧论文页眉、关键词及局部语法。

## 定稿前需要核实

1. 仿真 69.3% 对应标准划分还是同类别未见实例？目前没有分别汇报两种协议，不能同时声称两种结果。
2. Put Object into Cabinet 的成功条件是否包括放入物体？当前正文只描述开门，需与实际评测脚本核对。
3. 完整模型是否确实从观测物体云独立抽取 support/query？早期稿件曾写从 support 中抽 query，必须与实际实验代码核对，不能仅靠改文字定义区别。
4. 提供训练 seed 数、优化器、学习率、扩散步数和 rollout 上限；消融均值也应提供跨 seed 变异性。
5. G3Flow：四任务结果、标准差、实现来源、方法适配、重建/观测要求及训练配置待补。
6. DINOv2 lifting：特征层、图像分辨率、投影/遮挡处理、多视角融合规则和特征归一化仍需说明。
7. PCA 图是否每个方法、每个类别使用跨实例共享的投影基与颜色缩放？当前文字如此描述，需按实际绘图流程确认。
8. 特征一致性、part mIoU 等定量指标和受控噪声实验仍未呈现，不应视为已经完成。
9. real_world_experiment.png 内部两列重复使用 (c)，simulation_tasks.png 内部 (a)/(b)/(C)/(D) 大小写不统一；请修改图片源文件。
10. 当前逐类别监督、遮挡与物理属性限制已保留；全文页数待最终图表大小确定后压缩。
